@page
@model Bereschi_Gabriel_Depozit.Pages.Depozite.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Depozit[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Depozit[0].Adresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Depozit[0].Capacitate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Depozit[0].Proprietar.Nume)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Depozit) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capacitate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proprietar.Nume)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
